# 其他分类与指定错刊删除

## 用户要求

- 明确非研究内容（诺贝尔讲座、会长演讲、编委名单、目录、已核实期刊公告，以及来源一致明确标为社论/书评的内容）归入“其他”。
- 删除 IAEME 的 `10.34218/jom_13_02_005`，而不是 SAGE Journal of Management 的研究论文。

## 边界与兼容

分类版本 4、Master List 新写入版本 7。原版本 1–6 仍按原来的规则重建，不重写历史快照。原文、已有译文和首次发现时间不变。“其他”不进入研究论文自动补全和翻译队列；完整历史翻译队列的计算规则不变。更正和撤稿保留单独的警示分类；有来源冲突或类型不明的记录仍待核实。旧 `kind=administrative` 链接兼容跳转到“其他”。

删除授权版本 2 只新增这一条 JM DOI；旧的五条 JAR 删除历史继续按授权版本 1 验证。其余已隔离记录和所有正常研究论文不删除。新快照保留父快照及明确的删除报告，可恢复。DOI/ISSN 入口校验及已确认错刊拒收规则继续有效。

新增默认只读的 `scripts/correct-journal-identities.js`；仅现有默认分支后台传入 `--save` 才执行上述固定清单。复用既有共享生产锁、正式数据验证与 Git 保存，不新增权限、密钥或付费 API 调用。现有手动翻译发布任务在翻译前执行此校正。
