# 公开仓库＋公开阅读网站：首次上线准备

更新日期：2026-09-10。代码与正式历史已上传，首次服务器手动试运行已核对；现有492条记录、5篇完整中文样本。测试、保存和构建通过，两条无标题来源记录使整体报告部分失败；网站和每日日程仍未启用。最新状态见[手动试运行核对](github-trial-2026-09-09.md)。下文保留准备过程的时间顺序。

## 当前已经确定

用户最终明确：论文资料本来是公开信息，不需要保密；采用**公开GitHub仓库＋公开、免登录、只读网站**。代码、正式论文记录、来源原始记录与正式历史可以在同一公开仓库保存，不再拆分私有数据或实现团队登录。

中途加入的“必须使用私有仓库”限制已经取消。公开范围已经确定，不再重复问用户是否公开。仍遵守协作原则：一般代码、检查和准备工作自主处理；需要账户／密钥、费用或实质取舍时再询问，临近正式网站发布前请用户确认。

## 文件范围

- 公开仓库：本项目代码、必要说明、19刊配置及校验通过的正式论文历史，含双方来源原始记录。
- 公开网页：现有静态构建生成的8个文件，展示论文、中文译文、来源、日期、分类和状态。网页不提供修改数据或触发采集的权限。
- 不上传：密钥、个人账号配置、旧工具阅读／收藏记录、翻译草稿及未正式保存的工作尝试。

保留Git对运行数据的默认排除，通过正式历史清单显式加入合格文件，不加入整个数据目录。保留资料来源标注；本轮没有进行独立法律核验。

## 上传前准备（历史记录）

1. 两份流程模板取消私有仓库检查，保留校验、最小权限、默认分支及独立启用开关。
2. `.github/workflows/daily-collect.yml`和`deploy-pages.yml`已准备在本地，只有手动入口；没有推送触发和定时日程。模板日程仅供手动验收后再添加。
3. 上传组件按官方记录更新到v5.0.0固定提交，并保留网页所需隐藏标记；始终只上传已校验的网页目录。[官方定义](https://raw.githubusercontent.com/actions/upload-pages-artifact/v5.0.0/action.yml)
4. 全套184项测试通过；正式库2个版本、50个文件的历史链校验通过。论文仍为487条，中文完成5篇；本轮未采集、重译或修改正式库。
5. 对157个拟纳入版本控制的候选文件做常见密钥模式扫描。最初2处位于参考文献普通词内部，是宽松前缀匹配；核对JSON位置及词边界后排除，未输出可疑值。带词边界的复查没有命中常见凭据格式。这是有限模式扫描，不代表绝无敏感信息，最终提交前仍检查实际清单。

这些是本地验证，不是GitHub实机运行或部署成功。

## 设备登录前核对的远端状态（历史记录）

浏览器实际查看的仓库为`w1121188104w-hue/paper-daily`，公开、Fork自`limafang/paper-daily`；默认分支为`master`，远端仍是原项目版本。

只读分支查询确认远端与本地HEAD同为`8a8aa6079984d743973d4ea858c840c0b3183ba9`。没有拉取、暂存、提交、推送或修改分支。

工具附带Git的HTTPS辅助程序位于`mingw64/bin`，本轮通过单次命令指定正确位置完成只读联网查询，未安装软件、更改全局Git配置或存储凭据。

浏览器显示“Sign in”，尚未登录，因此不能核实当前用户的管理权限、Actions开关、Pages设置或保护规则。公开仓库可读取不等于拥有上传权限。

## 登录后的进展与后续

以下步骤1至4已完成。已通过正常上传保留旧历史，并仅启用手动采集；远端结果已安全同步回本地，正式历史3个版本、93个文件通过校验，全套185项测试再次通过。步骤5等待正式网站发布前的用户确认；不会因笼统的“继续”自行开启网站或每日日程。

1. 用户在GitHub正常登录，不把密码、验证码或访问令牌发到聊天中。
2. 核对归属、管理权限、现有Actions／Pages设置与保护规则；复核准备提交的文件。
3. 通过正常授权的连接上传代码和正式数据，保留原历史；若需额外授权，先说明用途，不自行创建密钥。
4. 先仅启用手动采集，不启用Pages发布或日程。核查线上采集、保存和静态构建，来源失败如实记录，不清空历史。
5. 临近正式公开网站时请用户确认，再开启Pages发布和验证实际网页；最后才添加每天自动运行。

不创建私有仓库，不购买套餐，不绕过权限或分支保护。
