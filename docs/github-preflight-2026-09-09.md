# GitHub 首次上传前核验（2026-09-09）

本记录是上传前检查，不代表线上试运行或网站发布成功。正式公开网站仍需用户最后确认。

## 已完成的账户与仓库检查

- 用户已登录。GitHub页面明确显示当前用户拥有`w1121188104w-hue/paper-daily`，可以进入仓库设置。
- 仓库公开，默认分支`master`，保留原项目的Fork关系和提交历史。
- 上传前远端为`8a8aa6079984d743973d4ea858c840c0b3183ba9`。
- GitHub Pages明确显示disabled，来源分支为None；没有启用或修改网站发布。
- 没有经典分支保护，也没有Rulesets；不需要绕过保护规则。
- Actions允许现有官方组件；默认运行令牌仍为只读内容与包。保持此默认，采集任务只在自身工作流内申请所需内容写入权限。
- 环境变量和仓库变量页面均为空，`JOURNAL_AUTOMATION_ENABLED`与`JOURNAL_PAGES_ENABLED`均未设置。
- 电脑端原先没有Git上传登录。经用户批准启动设备登录后，GitHub显示设备连接成功，Git Credential Manager正常退出；随后不上传文件的`push --dry-run`通过。没有创建个人访问令牌，没有将凭据写入项目。

## 本地数据与上传范围

- 185项测试通过，其中新增真实临时Git仓库测试：LF及CRLF原始记录在`core.autocrlf=true/input/false`检出后保持完全一致。
- `.gitattributes`仅对`data/journal-store/**`禁用文本换行转换，防止正式历史的内容校验值在Windows与Linux间变化。
- 正式历史仍为2个版本、50个文件、487条论文记录、5篇已完成中文样本；本轮未采集或修改正式论文内容。
- 上传代码、必要文档、期刊配置和校验清单指定的正式历史。继续排除密钥、账号配置、旧工具个人阅读数据、翻译草稿和未保存的尝试记录。
- 首次上传使用现有默认分支的普通提交与推送；不强推、不重写历史。提交署名使用已核实的GitHub账号和noreply地址，不更改全局Git身份设置。
- `.github/workflows/`只含手动入口，无定时或推送触发；上传不等于启动采集，更不等于发布网站。

## 上传后顺序

先核实远端文件及提交，再仅启用手动采集，保持Pages和每日日程关闭。检查线上测试、来源响应、数据保存及网页构建；临近正式网站发布时再请用户确认。
