# 本地验收通过；尚未正式上线

日期：2026-09-09。范围：本机程序、现有正式数据、实际浏览器交互和静态网页包。未做新的采集或翻译，未更改密钥、Git暂存区、远端仓库、工作流开关或部署设置。

后续决定：用户进一步选择公开仓库＋公开网站，正式论文资料也可公开；已移除中途加入的私有仓库限制，准备仅手动触发的工作流。最新范围见 [公开阅读方案](public-reading-scope.md)。下方181项测试及“是否公开待确认”保留为当时记录；最新184项通过，浏览器未登录GitHub，网站仍未部署。

## 给普通用户的结论

目前可以在本机正常看论文、按期刊和类型筛选、搜索中文或英文、切换摘要语言、翻页，以及按首次发现日期查看论文。准备上传的网站文件已包含最新5篇中文样本和本次页面修复，但没有上传。下一步涉及谁能访问网站、数据保存在哪个仓库，需要先确认公开范围，不能把本地检查通过称为正式上线成功。

本次沿用原来的绿色日历和卡片布局，网站技能用于本地浏览器验收与静态文件检查，没有重新设计网站或触发托管发布。

## 本次改进

1. **发表日期有来源说明。** 保留来源实际提供的精度：只有年份就显示年份，只有年月就显示年月，提供完整日期则保留完整日期。AER样本中的“1日”确实存在于Crossref原始日期中，不是程序补出来的；因此没有改动正式日期，页面标注“数据库标注日期，未逐篇核实到日”。首次发现日期仍按北京时间归档。
2. **作者不同写法可对照。** 例如雅加达公交论文在Crossref中为“Benjamin A. Olken”，在OpenAlex中为“Benjamin Olken”。两份名单分别展示，保留各自顺序；完整姓名也能搜索到论文。不猜测身份，不把不同名单拼成新名单，不改主记录。
3. **避免多余差异提示。** 仅有Unicode组合、大小写或连字符字形不同的名单不重复提示；顺序、人数、重音或姓名实质不同仍保留差异。

## 实际页面检查

使用本机只读预览及真实已保存的487条记录，不用虚构数据冒充正式结果。

| 检查 | 实际结果 |
| --- | --- |
| 5篇中文样本逐篇搜索 | “人才囤积”“快速公交系统”“远程办公”“职业动机”“主观信念”各找到对应1篇 |
| 中文摘要加期刊筛选 | AER + “候车时间”找到雅加达公交论文；关键词位于中文摘要 |
| 来源作者名搜索 | “Benjamin A. Olken”找到1篇，对照中可见两个来源的完整署名 |
| 摘要切换 | 雅加达论文中文与完整英文可切换，默认中文 |
| 翻页 | AER研究候选22条，第一页20条、第二页2条；翻页后键盘焦点回到列表标题 |
| 文献类型 | 全部487条；研究候选471条；期刊资料10条；疑似更正5条；疑似撤稿1条；待人工核查0条 |
| 首次发现日 | 9月8日日历及日期详情均为471条候选，显示采集不完整 |
| 尚未采集日 | 9月9日详情为0条，明确显示未采集，不显示采集成功 |
| 返回日历 | 保留期刊、文献类型、中文或作者搜索词及月份 |
| 浏览器诊断 | 本轮观察未记录控制台警告或错误 |

没有命中的搜索也正常显示0条。本轮曾用不在样本中的“直接下属”搜索，结果为0；改用已在摘要中确认存在的“候车时间”后命中，未将无命中误判为搜索故障。

### 页面尺寸

设置浏览器视口为320×800、390×844、768×1024、1366×900进行检查。页面存在竖向滚动条时，可用内容宽度分别为305、375、753、1351像素；对应页面的横向滚动宽度与内容宽度一致，未见横向溢出。

320像素检查首页日历和日期详情；390像素检查展开作者差异的中文长卡片；768像素检查平板布局；1366像素检查电脑布局及最终静态页面。已观察截图和页面尺寸，不等于在实体手机、Safari或所有浏览器中完成兼容性认证；本轮未测试200%文字缩放。

## 自动检查和数据保护

实际执行并通过：

```powershell
node --test
node scripts/journal-preview.js --check
node scripts/journal-git-files.js --check
node docs/aer-pilot-2026-09-09/verify-saved.mjs
node scripts/build-journal-site.js
```

- 自动化测试：181项通过，0项失败；日期来源、作者对照及对应搜索共新增9项回归测试。
- 正式历史链检查：2个正式版本、50个应保留文件通过；没有执行暂存、提交或推送。
- 中文保存复核：仍为5篇、10个已完成字段，采集日志仍只有1次，翻译导入仍只有1次；旧历史和工作文件校验值保持一致。
- 当前总待翻译482条、804个字段；其中默认可导出466条研究候选、788个字段，其余16条暂缓。
- 本次展示修复没有改写正式库；没有重新采集、导入或批量修改作者、日期等元数据。

## 最终静态包

本次构建编号：`20260909T064158281Z-d98316c6-4ca5-414d-8328-d76a670dcac7`。

本地目录：`data/site-builds/versions/20260909T064158281Z-d98316c6-4ca5-414d-8328-d76a670dcac7/site/`。

只有8个文件：`index.html`、`day.html`、`app.js`、`viewModel.js`、`styles.css`、`base.css`、`data.json`、`.nojekyll`。文件清单及逐文件校验值通过，包含487条记录及5篇完整中文样本。网页数据版本时间与正式版本的`created_at`一致：`2026-09-09T01:23:25.585Z`；它不同于翻译导入开始时间，不能混用。

网站包采用公开字段白名单，没有采集原始响应、内部版本路径、翻译工作批次或密钥文件。`data.json`约1.15MB；未做更大历史规模的性能测试。

已用仅监听本机的临时服务器，在`/paper-daily/`子目录中实际访问这一份构建文件，确认样式、脚本和数据可加载，中文摘要搜索与日期详情跳转正常。此验证没有访问GitHub Pages，也没有把数据发送到外网。

## 上线前仍需处理

1. **确认公开范围。** 网站是否允许任何拿到链接的人访问？网页中的论文标题、作者、英文摘要和中文译文是否允许对外展示？公开可见不自动等于取得了再发布许可，相关权利依据也需在发布前核对。
2. **确认正式数据的保存范围。** 当前模板会把完整正式历史（包括来源原始记录）提交到同一个代码仓库；仓库若公开，这部分也会公开。网站包精简不代表仓库中的数据也精简。未获得明确授权前不上传正式库。
3. **验证GitHub实机流程。** 本地配置的远端是`w1121188104w-hue/paper-daily`，本轮未核实当前登录身份、仓库可见性或权限。获准后先核对权限和分支保护，再手动试运行，验证重新发布后数据仍保留，最后才考虑开启每日任务。

5篇中文只是首批样本，不代表全库已翻译；英文先展示符合V0.1需求。JAR与JF的缺标题来源条目仍保留在异常记录中，本次没有假称已经补齐；来源信息和候选分类尚未逐篇人工确认。

临时本地验收服务在检查结束后关闭。需要再次看本地现有数据，可运行`node scripts/journal-preview.js`；不要启动旧的arXiv服务。
