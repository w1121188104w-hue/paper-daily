# 两轮搜索：2026-09-13 开发记录

用户已配置 GitHub Secrets `ZHIPU_API_KEY`、`SERPAPI_API_KEY`，只读核对名称均存在；没有下载、回显或写入密钥。用户授权智谱 `search_pro`、每自然月最多 2000 次；SerpAPI 两个引擎合计最多 250 次且仅允许真实免费套餐。无自动支付、续费、升级。

开发副本：`G:/自动收集论文/paper-daily-search`，分支 `codex/master-list-search`，从远端 `2fb898f23b0f254c159a8b28251c89340b9ceea6` 建立。原工作目录和未提交改动保留。只迁入上一批 Master List 代码，不复制旧论文数据。

流程：三源独立采集 + 直接读取官网清单 → 搜索补齐期刊目录/在线优先发表清单 → 合并总名册 → 三源补元数据 → 搜索单篇缺失字段 → DeepSeek 翻译。

第一轮按期刊和月份独立搜索；搜索结果只是网页线索。按允许的出版社域名读取原始目录，继续分页、相关期次和文章身份校验，不能找到一篇就停止。遇到限制保存待查网址并标记部分覆盖，不声称清单已完整。明确期刊身份及结构化标题的官网新论文允许暂缺 DOI。

第二轮先查 Crossref、OpenAlex、Semantic Scholar，再按智谱 → Scholar → Google 查已知论文。只能采用真实数据源或原始出版社页面，不得采用搜索 snippet 或生成文本作为 Abstract。不改写已有摘要和中文译文，不覆盖已有 DOI，月份冲突暂停该条修复。

费用账本独立保存至 `codex/search-ledger` 分支的 `data/search-budget.json`。每次收费请求前用 GitHub Contents SHA 比较并交换，先确认远端预占，再调用；失败结果不明继续占用额度。并发写入冲突即停止，不强推。已有账本丢失不得当成零用量。此分支独立于论文正式历史，防止预占提交导致采集数据上传版本落后。

手动连通性验证只查 SerpAPI 免费账户状态，并尝试 1 次智谱 Pro 清单查询；不调用 SerpAPI 搜索、不修改论文、不翻译、不部署网站。生产开关保持 false，完整每日自动接线、缓存与正式历史写入需继续验收。

## 初始验收与权限边界（用户单独授权之前的历史记录）

- 离线测试 320 项通过，0 失败。最新远端的 545 篇正式论文通过只读校验，数据目录没有改动。
- 开发代码已保存到远端独立开发分支，提交 `d6f3c3daafb0d9383ad32912bc51d068dd599892`。没有合并生产分支，没有切换每日采集、翻译或页面部署开关。
- `.github/workflows/search-preflight.yml` 仅在本地准备完成，锁定上述已测试提交；没有上传成功、没有执行。
- 安全检查拒绝向默认分支新增带密钥访问和 `contents:write` 权限的工作流，要求用户单独授权。该权限只计划用于在独立账本分支保存调用前预占和调用后结算；但仍属于仓库写入权限，不能假装只读。
- 因此目前只有“密钥名称存在”的验证，没有真实连通性、余额或搜索命中验证；实际智谱/SerpAPI搜索调用均为 0。不得把测试模拟的免费账户结果当成用户实际账户状态。
- 第一期刊清单搜索、第二字段搜索目前为经过测试的核心模块；正式库写入、跨运行缓存、每日自动接线尚未全部完成，不应声称已上线。

## 用户授权后：实际验证结果与修复

用户随后明确同意建立并运行手动验证工作流，之后再次要求继续。默认分支仅新增/更新 `search-preflight.yml`，该工作流不发布网站。核心搜索功能仍保留在独立开发分支，`production_enabled` 仍为 false。

1. 最初两次工作流在第一次智谱请求之前失败；只读核对确认当时还没有搜索账本分支。第二次运行使用了旧的默认分支快照，不能当成新版诊断结果。问题最终定位到 GitHub 仓库根 API 地址多出尾斜杠，与用户的搜索密钥无关；已在 `30a1898c3bb0b168b65184f77407ab57f6701aad` 修复并添加回归测试。
2. [账户只读诊断 34755154786](https://github.com/w1121188104w-hue/paper-daily/actions/runs/34755154786) 真实确认 SerpAPI：Active、免费方案、每周期 250 次、已用 0、剩余 250、额外付费额度 0。没有调用 SerpAPI 搜索。
3. [单次 Pro 验证 34755377988](https://github.com/w1121188104w-hue/paper-daily/actions/runs/34755377988) 使用修复后的代码，323 项测试通过；已发送 **1 次** 智谱 `search_pro` 请求，但结果为 incomplete，没有取得可验证的搜索结果。SerpAPI 当时仍为已用 0、剩余 250，实际续期日为 2026-10-13。
4. 独立账本已创建并只读核实：智谱记录 1 条，状态 `unknown`，`charged = null`，预占时间 `2026-09-13T11:48:43.391Z`，结算时间 `2026-09-13T11:48:44.807Z`。这条记录保守占用 2000 次月上限中的 1 次；它**不证明实际扣费**，不得释放或覆盖为零。
5. 旧脚本丢弃了 HTTP 状态及远端错误码，不能据此判断是余额、认证、限流还是接口响应问题。尝试只读打开智谱消费记录页面时浏览器超时，未取得后台证据，也没有操作充值、密钥或账号设置。

本轮没有修改正式论文、没有调用 DeepSeek、没有部署网站，也没有启用每日搜索。

## 安全诊断补丁（仅离线测试，尚未再次调用 API）

- 搜索响应与错误响应统一限制读取大小和超时；只保留 HTTP 状态、固定本地错误码和白名单中的智谱标准业务错误码，丢弃远端消息、URL、密钥、账户个人信息。
- 保留错误诊断不等于已核实计费：失败仍按未知结果保留预占，不自动重发，也不重置历史账本。
- 手动验证报告新增是否发出请求、阻止原因和安全错误码。模拟错误码（例如测试中的 1113）**不是用户真实请求的错误码**。
- 完整离线测试 **328 项通过，0 失败**。正式 `data` 目录、`package.json`、`package-lock.json` 均无改动。
- 原约定的 1 次真实 Pro 验证已经使用；本补丁未触发第二次请求。若需要实测错误码，应先取得用户对额外 1 次诊断请求的同意，并继续计入同一个月度账本。
- 正式库写入、跨运行缓存、每日自动接线仍待后续完成；本记录不得被理解为完整流水线已经上线。
