# 两轮搜索：2026-09-13 开发记录

最新状态：用户修正 GitHub 的 `ZHIPU_API_KEY` 后，2026-09-13 20:34（北京时间）真实 Pro 搜索已成功；详细结果见文末。下文早期认证失败是旧密钥的历史记录，不再是当前阻塞。完整每日搜索流水线仍未上线。

用户已配置 GitHub Secrets `ZHIPU_API_KEY`、`SERPAPI_API_KEY`，只读核对名称均存在；没有下载、回显或写入密钥。用户授权智谱 `search_pro`、每自然月最多 2000 次；SerpAPI 两个引擎合计最多 250 次且仅允许真实免费套餐。无自动支付、续费、升级。

开发副本：`G:/自动收集论文/paper-daily-search`，分支 `codex/master-list-search`，从远端 `2fb898f23b0f254c159a8b28251c89340b9ceea6` 建立。原工作目录和未提交改动保留。只迁入上一批 Master List 代码，不复制旧论文数据。

流程：三源独立采集 + 直接读取官网清单 → 搜索补齐期刊目录/在线优先发表清单 → 合并总名册 → 三源补元数据 → 搜索单篇缺失字段 → DeepSeek 翻译。

第一轮按期刊和月份独立搜索；搜索结果只是网页线索。按允许的出版社域名读取原始目录，继续分页、相关期次和文章身份校验，不能找到一篇就停止。遇到限制保存待查网址并标记部分覆盖，不声称清单已完整。明确期刊身份及结构化标题的官网新论文允许暂缺 DOI。

第二轮先查 Crossref、OpenAlex、Semantic Scholar，再按智谱 → Scholar → Google 查已知论文。只能采用真实数据源或原始出版社页面，不得采用搜索 snippet 或生成文本作为 Abstract。不改写已有摘要和中文译文，不覆盖已有 DOI，月份冲突暂停该条修复。

费用账本独立保存至 `codex/search-ledger` 分支的 `data/search-budget.json`。每次收费请求前用 GitHub Contents SHA 比较并交换，先确认远端预占，再调用；失败结果不明继续占用额度。并发写入冲突即停止，不强推。已有账本丢失不得当成零用量。此分支独立于论文正式历史，防止预占提交导致采集数据上传版本落后。

手动连通性验证只查 SerpAPI 免费账户状态，并尝试 1 次智谱 Pro 清单查询；不调用 SerpAPI 搜索、不修改论文、不翻译、不部署网站。生产开关保持 false，完整每日自动接线、缓存与正式历史写入需继续验收。

## 初始验收与权限边界（用户单独授权之前的历史记录）

- 离线测试 320 项通过，0 失败。最新远端的 545 篇正式论文通过只读校验，数据目录没有改动。
- 开发代码已保存到远端独立开发分支，提交 `d6f3c3daafb0d9383ad32912bc51d068dd599892`。没有合并生产分支，没有切换每日采集、翻译或页面部署开关。
- `.github/workflows/search-preflight.yml` 仅在本地准备完成，锁定上述已测试提交；没有上传成功、没有执行。
- 安全检查拒绝向默认分支新增带密钥访问和 `contents:write` 权限的工作流，要求用户单独授权。该权限只计划用于在独立账本分支保存调用前预占和调用后结算；但仍属于仓库写入权限，不能假装只读。
- 因此目前只有“密钥名称存在”的验证，没有真实连通性、余额或搜索命中验证；实际智谱/SerpAPI搜索调用均为 0。不得把测试模拟的免费账户结果当成用户实际账户状态。
- 第一期刊清单搜索、第二字段搜索目前为经过测试的核心模块；正式库写入、跨运行缓存、每日自动接线尚未全部完成，不应声称已上线。

## 用户授权后：实际验证结果与修复

用户随后明确同意建立并运行手动验证工作流，之后再次要求继续。默认分支仅新增/更新 `search-preflight.yml`，该工作流不发布网站。核心搜索功能仍保留在独立开发分支，`production_enabled` 仍为 false。

1. 最初两次工作流在第一次智谱请求之前失败；只读核对确认当时还没有搜索账本分支。第二次运行使用了旧的默认分支快照，不能当成新版诊断结果。问题最终定位到 GitHub 仓库根 API 地址多出尾斜杠，与用户的搜索密钥无关；已在 `30a1898c3bb0b168b65184f77407ab57f6701aad` 修复并添加回归测试。
2. [账户只读诊断 34755154786](https://github.com/w1121188104w-hue/paper-daily/actions/runs/34755154786) 真实确认 SerpAPI：Active、免费方案、每周期 250 次、已用 0、剩余 250、额外付费额度 0。没有调用 SerpAPI 搜索。
3. [单次 Pro 验证 34755377988](https://github.com/w1121188104w-hue/paper-daily/actions/runs/34755377988) 使用修复后的代码，323 项测试通过；已发送 **1 次** 智谱 `search_pro` 请求，但结果为 incomplete，没有取得可验证的搜索结果。SerpAPI 当时仍为已用 0、剩余 250，实际续期日为 2026-10-13。
4. 独立账本已创建并只读核实：智谱记录 1 条，状态 `unknown`，`charged = null`，预占时间 `2026-09-13T11:48:43.391Z`，结算时间 `2026-09-13T11:48:44.807Z`。这条记录保守占用 2000 次月上限中的 1 次；它**不证明实际扣费**，不得释放或覆盖为零。
5. 旧脚本丢弃了 HTTP 状态及远端错误码，不能据此判断是余额、认证、限流还是接口响应问题。尝试只读打开智谱消费记录页面时浏览器超时，未取得后台证据，也没有操作充值、密钥或账号设置。

本轮没有修改正式论文、没有调用 DeepSeek、没有部署网站，也没有启用每日搜索。

## 安全诊断补丁（仅离线测试，尚未再次调用 API）

- 搜索响应与错误响应统一限制读取大小和超时；只保留 HTTP 状态、固定本地错误码和白名单中的智谱标准业务错误码，丢弃远端消息、URL、密钥、账户个人信息。
- 保留错误诊断不等于已核实计费：失败仍按未知结果保留预占，不自动重发，也不重置历史账本。
- 手动验证报告新增是否发出请求、阻止原因和安全错误码。模拟错误码（例如测试中的 1113）**不是用户真实请求的错误码**。
- 完整离线测试 **328 项通过，0 失败**。正式 `data` 目录、`package.json`、`package-lock.json` 均无改动。
- 原约定的 1 次真实 Pro 验证已经使用；本补丁未触发第二次请求。若需要实测错误码，应先取得用户对额外 1 次诊断请求的同意，并继续计入同一个月度账本。
- 正式库写入、跨运行缓存、每日自动接线仍待后续完成；本记录不得被理解为完整流水线已经上线。

## 后续授权：允许继续诊断（2026-09-13）

用户明确允许失败后继续重试，说明已经充值 10 元、可以使用这笔余额完成连通性验证。因此，之前“再调用一次需要另行询问”的临时限制已被此次授权替代；仍不自动充值/升级，仍保留每月 2000 次上限，不为凑次数而盲目重复调用。

新增显式手动重试选项 `retry_unknown`：仅适用于 `workflow_dispatch`，需要有效 GitHub 运行编号，每轮最多发 1 次 Pro 请求；新运行可以保留旧预占后再次诊断，同一运行重复执行仍受冷却保护。默认仍为只读账户检查，重试默认关闭。正式采集的重试策略不受影响。

代码 `10e3c1ed0e0bf05b74fbe1e5bc60b4f2f0708514` 已通过 329 项离线测试。默认分支手动验证工作流固定到该提交，其余生产功能保持不变。

[再次诊断 34756268061](https://github.com/w1121188104w-hue/paper-daily/actions/runs/34756268061) 实际返回 HTTP 401 / `ACCESS_RESTRICTED`，未取得搜索结果；当时业务错误码不在已知投影中，不能猜测具体代码。账本累计 2 条未知结果预占，SerpAPI 仍未执行搜索。

随后补充官方旧版文档中的 1002 / 1004 认证错误码，并新增仅在运行器内存中计算的密钥格式诊断。输出只有固定枚举和“是否与 SerpAPI 密钥相同”的布尔值，不输出字符、长度、哈希或解码内容；格式看似正常不代表密钥有效。版本 `4bbf83c41c912e9f497ff6a20982f0c5a60c34c1` 已通过 330 项离线测试，手动工作流已固定到此版本。中途上传被打断，经核对后恢复，未重复发出搜索请求。

[认证诊断 34757149758](https://github.com/w1121188104w-hue/paper-daily/actions/runs/34757149758) 再次实际返回 HTTP 401 / `ACCESS_RESTRICTED`，业务码未能识别。密钥格式为 `opaque`，与 SerpAPI 密钥不同；未发现空格、包裹引号、URL 或明显掩码，但这些检查不证明密钥有效，也不能确定其具体产品类型。端点和 `Authorization: Bearer API_KEY` 写法与[智谱官方 HTTP 文档](https://docs.bigmodel.cn/cn/guide/develop/http/introduction)一致。

最终只读核对：独立账本累计 **3 次智谱请求**，均为 `unknown / charged=null`；实际扣费未知，继续保守计数。SerpAPI 账户剩余 **250**，本轮及此前均无 SerpAPI 搜索调用。GitHub 两个 Secret 名称存在；没有读取其值。全部任务已结束，无等待中的后台重试。

当前阻塞为智谱密钥未通过身份验证，而非已验证的余额不足。下一步需用户在充值的智谱开放平台账户创建/复制完整通用 API Key，更新仓库的 `ZHIPU_API_KEY` 后，再继续已获授权的诊断；不要在聊天或代码中提供密钥。不盲目重复相同 401 请求，不替用户充值或改套餐。搜索核心开发未合并到生产、每日搜索未启用、正式论文/译文/网站均未修改。

## 新密钥验证成功（认证阻塞已解除）

用户确认先前 API Key 配置错误，保留同名 `ZHIPU_API_KEY` 并更新内容。只读查询 GitHub Secret 元数据确认更新时间为 `2026-09-13T12:32:58Z`，未读取密钥值；此时无尚未结束的搜索验证任务。

[成功验证 34757442674](https://github.com/w1121188104w-hue/paper-daily/actions/runs/34757442674) 使用相同代码 `4bbf83c41c912e9f497ff6a20982f0c5a60c34c1`，先通过测试，再发出 **1 次**新密钥 Pro 请求：

- 状态 `success`，引擎 `search_pro`，无错误诊断。
- 查询 `American Economic Review 2026-09 articles` 返回 47 条经过链接安全过滤的网页线索，其中 19 条来自 `www.aeaweb.org` / `pubs.aeaweb.org`。这是网页线索数，不是论文数或完整性证明；尚未用这些线索写入正式论文库。
- 远端账本核实共 4 条记录：旧密钥 3 条未知结果预占，新密钥 1 条成功。保守月计数 4 / 2000；成功请求按 1 次记账，不冒充已经核对账户人民币账单。旧 3 条实际扣费仍未知，不擅自释放额度。
- SerpAPI 账户仍为免费方案、已用 0、剩余 250、续期日 2026-10-13；没有调用 SerpAPI 搜索。
- 论文写入 0，DeepSeek 调用 0，网站部署 0。没有充值、升级或改动任何账号/密钥内容。

接口连通性目标已达到，停止为诊断重复调用。后续继续完成第一轮期刊清单搜索、Master List 写入，再衔接第二轮缺失字段搜索及现有翻译；上线前仍需完整验收。

## 两轮搜索的开发版保存与重试已接通

此阶段仅使用模拟数据及临时测试库，没有调用真实搜索、翻译或网页接口，没有新增 API 费用。没有切换 GitHub 默认分支、每日任务或网站部署。

第一轮入口 `runCatalogDiscovery`：现有三源独立采集应先执行；本入口随后读取官网清单，按近 60 天涉及的期刊/月份进行独立目录搜索，再把原始官方页面验证过的研究论文补入版本库。新论文允许 DOI、作者和发表月份暂缺；只知月份不伪造具体日期，边界月份及未知时间明确标记不确定，已确认超出窗口的论文不新增。已存在论文的 ID、首次发现时间、译文和规范字段均不改动；其官方发现标记来自核对报告，而不是一次摘要查询。

清单状态随原有 `enrichment-state.json` 一起校验、保存：同一北京时间日期不重复查询；次日重新读取记住的官方目录，能核实当前月份时不再请求收费搜索。失败网址、月份待办和原始核对报告保留，访问受限与未找到、额度耗尽分开。找到部分目录不声称完全覆盖。缓存保留最多 100 个受约束网址，优先目录与未完成网址；超过单次分页/文章读取上限仍标记不完整。

第二轮入口 `runMetadataRepair`：只处理到期的缺失 DOI、作者、月份、摘要字段；先结构化来源，再依次智谱、Scholar、Google。补全结果、来源证据、未解决事项及下一次尝试时间随同一个版本提交。找不到摘要时，中英文摘要都保持空；找到原始英文后进入原来的翻译队列，本入口不调用 DeepSeek。已有译文和已知字段不能改写，保存时必须能够从新追加的证据重演全部字段变化。

两轮入口都没有默认生产根目录、不读取环境变量/密钥、不隐式建立收费客户端；调用方必须显式提供目录及带远端预记账保护的搜索函数。免费额度耗尽的字段待办需要调用方提供已核实的未来额度重置时间。来源不可用可以保存其他来源的成果；额度账本或证据持久化失败则停止，不绕过记账换引擎继续。未写完的版本不替换旧的 `current.json`。

新快照显式保存 Master List 时间窗口；后续翻译/字段修复保留该窗口，下一次发现运行可滚动更新。旧快照没有这个字段时仍按原规则读取，避免破坏历史校验。独立目录缓存、待办和报告随原有历史文件引用链归档，不增加对整个 data 目录的盲目暂存。

只读命令 `node scripts/master-list.js --unresolved` 现在同时提供字段待办、清单查询待办及尚不能录入的冲突论文；`--due` 按重试时间过滤，可供后续集中读取。仍然不是人工核查节点。

验收：**347 项测试通过、0 失败**，覆盖无 DOI/无作者/月或年日期入库、去重与冲突、同目录跨月份复用、次日不重复收费、来源故障隔离、两阶段版本写入、真实来源摘要入翻译队列、配额与重试、历史不可覆盖、保存中断保护。开发副本中的 545 篇已有论文通过只读兼容性校验，正式 `data`、密钥配置、依赖锁文件均未修改。

仍待完成：把三源采集、这两个新入口、真实预算客户端和翻译调用按完整顺序接入受控试跑；对真实出版商响应作小批联合验收；补全统一的本轮发现/确认/额度统计；再切换每日生产任务。身份/分类冲突仍保留为自动待办，不能把尚未实现的冲突消解声称已经完成。当前两轮写入的验证均为临时测试库，不是已经为线上新增或修复论文。

## 隔离联合试跑入口

新增 `scripts/search-pilot.js --run`，只允许本仓库 GitHub 手动任务执行。按校验过的历史引用链复制论文库到仓库外的新临时目录，三源独立采集 → 官网目录/月份搜索 → 最多 3 篇缺失字段修复 → 原翻译队列校验。每阶段只向副本写入，结束时重新比对原库所有被复制文件的哈希及正式指针。试跑不读取 DeepSeek 密钥、不调用翻译、不提交论文数据、不构建或发布网站。

范围固定 AER、滚动 60 天；单轮最多 6 次 Pro、4 次两引擎共享的免费 SerpAPI 搜索，并继续遵守远端先记账及 2000/250 月上限。每次 SerpAPI 搜索前刷新免费账户核验。失败和结果不明的调用也占用试跑上限；单轮达到上限标记 `pilot_request_limit`，不冒充月额度耗尽。公共网页最多 90 次请求，尊重 robots 和限流；有分页及 6 分钟新请求截止保护。截断/受限结果保留未完成状态，不证明期刊清单完整。

新增 6 项隔离与预算测试，全套 **353 项测试通过**。此条记录是试跑入口的本地验收；真实联合试跑结果须另行记录，不能由测试通过推断真实网站可访问或论文已补齐。

## 2026-09-14 首轮真实联合试跑

手动任务 `34797668870` 成功完成，固定代码 `216a99fb6857c07755daa5338f89b401170f3dae`；353 项离线测试通过后才访问搜索密钥。三源独立发现研究候选数量：Crossref 22、OpenAlex 13、Semantic Scholar 10，去重并集 22。三源无新增，但更新了副本中 10 篇的来源信息。Semantic Scholar 原始 46 条是年度查询返回量，不是 60 天内论文数量。

官网阶段在副本中新增 8 条，副本全库从 545 到 553。AER 最终共 30 条，其中月份窗口能确认在内 11 条、边界/未知 19 条；因此不能把这 8 条直接宣传为“60 天内确定漏收”。目录状态为 partial，实际访问受限和试跑分页/调用上限均有记录。首轮汇总未携带新增条目的标题和日期依据，后续补上逐条公开元数据输出再核对。

本次共新增 5 篇具有英文摘要的记录并产生 5 项摘要翻译待办；不等同于第二阶段修复了 5 篇。第二阶段选择的 3 篇仍缺摘要，Crossref/OpenAlex 未提供新字段、Semantic Scholar 限流、搜索未能解决且触达试跑 SerpAPI 上限。`papers_changed=3` 仅表示来源证据记录发生变化，`changed_fields` 全空，实际补齐字段的论文数为 0。继续保留缺失/重试状态，不生成摘要。

真实调用：Pro 6 次、免费 SerpAPI 4 次。远端账本本月保守累计 Pro 10/2000（包含旧密钥 3 次计费不明），SerpAPI 4/250。正式库所有复制文件及指针哈希未变，DeepSeek 调用 0，网站部署 0。生产自动采集逻辑仍未切换。

## 2026-09-14 新增记录明细复核

第二轮任务 [34798085402](https://github.com/w1121188104w-hue/paper-daily/actions/runs/34798085402) 成功完成，固定代码 `33b627fbd1f0b1b8b07b65d1c9fe14cf9e98ee6a`。**354 项测试全部通过**。此次报告增加新增论文的标题、DOI、作者、原日期、窗口状态、官方页面及证据哈希；原摘要正文不写进控制台。报告另列实际有字段补齐的论文数，避免将仅追加来源证据当成补全成功。

两轮均由同一只读开发库创建独立副本，不是在第一轮副本基础上追加。第二轮再次发现相同 8 条，因此两轮不能相加成 16 篇。逐条核对得到：

| 标题 | DOI | 原始日期 | 官网可提取英文摘要 |
| --- | --- | --- | --- |
| A Welfare Analysis of Policies Impacting Climate Change | 10.1257/aer.20250166 | 2026-07 | 有 |
| Monetary Policy without Commitment | 10.1257/aer.20241925 | 2026-07 | 有 |
| Market Opacity and Fragility: Why Liquidity Evaporates When It Is Most Needed | 10.1257/aer.20231613 | 2026-07 | 有 |
| Understanding High-Wage Firms: Monopoly, Monopsony, and Bargaining Power | 10.1257/aer.20230344 | 2026-07 | 有 |
| Self-Fulfilling Fluctuations in HANK Economies | 10.1257/aer.20240642 | 2026-07 | 有 |
| Nobel Lecture: The Past and Future of Innovation: Can Progress Be Sustained? | 10.1257/aer.116.7.2299 | 2026-07 | 未提取到 |
| Nobel Lecture: The Economics of Creative Destruction | 10.1257/aer.116.7.2324 | 2026-07 | 未提取到 |
| Nobel Lecture: Creative Destruction and Economic Growth | 10.1257/aer.116.7.2357 | 2026-07 | 未提取到 |

全部新增条目的 `window_status` 都是 `boundary_date_uncertain`，不是能确定在 60 天内的漏收。官方论文证据可按 `https://www.aeaweb.org/articles?id=<DOI>` 追溯。5 个真实英文摘要全部来自官网原始正文，均进入原摘要翻译队列，本轮并未翻译。剩余 3 条实际是 Nobel Lecture；目前保守默认分类仍将其保留为候选，不能直接声称均为常规研究论文。未提取到摘要不等于已证明出版社永久不提供摘要，仍保留自动重试。

两轮真实调用合计：Pro 12 次、SerpAPI 免费搜索 8 次。远端账本交叉核对：智谱成功 13 次、旧密钥结果不明 3 次（保守总计 16/2000）；SerpAPI Scholar 成功 4 次、Google 成功 4 次（共享总计 8/250）。没有付费升级、充值、重配密钥或调用 DeepSeek。

已验收的是 AER 小批完整运行顺序、真实三源独立查询、官网新增记录写入副本、缺失字段失败保留、真实原文入翻译队列、远端额度保护。仍未验收全期刊完整覆盖、缺失摘要的真实补全成功样例、演讲等文章类型精分以及正式每日增量流程；本版本不据此切换生产。后续先完善边界日期/文章类型口径并扩展其他期刊小批测试，再接生产自动化。

## 统计口径 v2 与跨出版社试跑准备

总名册新增有版本的统计规则：新快照显式写入 `master_policy_version: 2`，旧快照没有该字段则严格按 v1 重算并验证，不重写旧文件或破坏历史哈希。新名册仍按月份展示，范围判断可使用既有来源中的真实日期；多个来源具体日不一致且跨过窗口边界时继续不确定，不随意选择一天。只有月份/年份的论文仍保留边界/未知状态，不补造日期。

新增 `document_type`/`research_candidate`，只将明确以 Nobel Lecture、Presidential Address 开头且来源一致的记录标成 `lecture`，保留论文及其元数据、原分类和现有 UI。演讲不再计入普通研究候选；标题中只是提及演讲不触发规则。标题来源相互矛盾时保留 uncertain，不伪装完成自动确认。新增统计分开列出演讲、确定窗口内研究候选、边界/未知研究候选以及窗口外研究候选；官网补入报告同样区分这几类并验证计数。

试跑支持已审核单刊 AER、QJE、JAR，一次只跑一本，所有阶段和汇总使用同一期刊参数，不扩大单轮 6 次 Pro/4 次免费 SerpAPI/3 篇字段修复上限。计划先验证 QJE 的 OUP 官网适配。原 UI、DeepSeek 调用逻辑、默认每日生产任务和正式论文数据不变。新增分类/日期/历史兼容及范围限制测试，全套 360 项通过；真实开发库旧历史通过只读兼容校验。

## QJE 跨出版社真实验证结果（2026-09-14）

[任务 34800507049](https://github.com/w1121188104w-hue/paper-daily/actions/runs/34800507049) 成功，固定代码 `344a3d1ac1a810e9866fc99661b46abd5b83a412`；云端 360 项测试通过后才读取密钥。因本地 Git 连接连续超时，开发提交通过官方 Git 对象接口上传，每个文件哈希、完整文件树及最终提交 SHA 均与本地一致，仅快进开发分支，无强制覆盖。每日生产和网站未切换。

三源独立查询结果：Crossref 8、OpenAlex 5、Semantic Scholar 4，去重并集 8；S2 原始年度查询 22 条不当作窗口内数量。现有库已包含这 8 条，三源未新增、更新了 4 条来源记录。

OUP 官方 RSS 发现并在临时副本新增 15 条有 DOI 和英文摘要、但缺作者/可靠发表日期的记录。来源是官方订阅数据里的明确 Abstract，不是搜索摘要；日期为空，因为订阅更新时间不能冒充原始发表日期。页面受限和分页上限继续保留 partial，不声称完整。15 条在发现时均为 `unknown_date`，确定 60 天内新增研究论文计数为 0，而不是 15。它们的原始英文摘要全部进入原翻译队列；没有调用 DeepSeek。

字段阶段仅检查 3 条，其中 2 条真正成功补齐作者及日期，来源均为 Crossref；补齐后无需付费搜索：

- `10.1093/qje/qjag018`：Technology Sophistication Across Establishments
- `10.1093/qje/qjag025`：International Reserve Management Under Rollover Crises

另一条 `10.1093/qje/qjag036` 仍缺作者和摘要：CR/OA没有新增字段，S2限流，搜索未解决，保留重试；没有生成摘要。`papers_changed=3` 与真正字段补齐的 `papers_with_fields_filled=2` 分开统计。

新规则下，副本 QJE 共 23 条（其中普通研究候选 22）：研究候选确定窗口内 7、日期仍不确定 13、已能判定窗口外 2。这个结果证明补齐日期可以修正窗口判断，而不是把所有新增条目永久当作近 60 天漏收。仍缺 DOI 0、作者 14、发表月份 13、摘要 1；自动待办是 14 篇/29 个问题，27 个尚未处理、2 个暂不可用。不能把 3 篇小批样本的完成当成全库补齐。

本轮 Pro 4 次、免费 SerpAPI 4 次；账本保守月累计 Pro 20/2000（含历史 3 次计费不明）、SerpAPI 12/250。副本全库 545→560，原库文件哈希和指针均未变，正式数据写入 0、翻译调用 0、网站部署 0。

下一步：继续扩大仍受上限保护的结构化元数据补全批量、验证其他出版社与跨日缓存/重试，然后统一接入每日流程。旧记录缺摘要的真实修复成功样例、所有期刊覆盖、标题冲突自动处理尚不能宣称完成。

## 20 篇批量与重复请求保护准备

手动隔离试跑批量只允许 3 或 20 篇，默认仍是 3，不能通过参数无限扩大；两者共享原单轮 6 次 Pro/4 次免费 SerpAPI、公共请求上限和截止时间。报告附字段修复后的月份、窗口状态、文章类型及摘要来源，避免只看到发现时未知日期而无法核对补全后的结论。

新增 `verifyPilotReuse`：重新读取同一个已保存副本，以全新、禁止联网的客户端再次走清单和字段入口。没有到期工作时，两段都必须返回跳过且正式指针不变；仍有因批量上限未处理的工作或已跨过午夜时，如实标记尚有待做事项、未验证该部分，不执行新网络请求，也不声称整个队列已清空。实际手动试跑默认执行这个立即复跑检查。

新增模拟时钟回归覆盖：字段失败后 24 小时内不查，到期后重新建立客户端并补入真实来源摘要，成功后多日不再请求；连续失败从 1 天延长到 3 天；清单在北京时间午夜进入下一次核对。跨日用的是离线模拟数据，不是已经跨越真实一天的线上验证。当前全套 **364 项测试通过**，未修改生产流程。

## QJE 扩大批量与即时复跑真实结果

[任务 34820362561](https://github.com/w1121188104w-hue/paper-daily/actions/runs/34820362561) 成功完成，固定代码 `1c97d177eeda35d9ce290e2b19818917d7b5b70d`。本轮设置上限 20 篇，实际有 16 篇到期缺字段记录：14 篇真正补齐作者及发表时间，2 篇未解决。`papers_changed=15` 仍不等于补全成功数，真实 `papers_with_fields_filled=14`。

副本 QJE 共 23 条，普通研究候选 22：确定窗口内 7、日期不确定 1、窗口外 14。官网初始发现的 15 条全是日期未知候选，补齐后不能当作 15 篇近 60 天漏收。这也不是在上一轮副本基础上另补 14 篇，而是从相同原库重建隔离副本后的扩大批量验证。

剩余 2 篇、5 个自动待办问题：

- `10.1093/qje/qjag036`：标题明确为 Correction to: A Further Augmentation of the Solow Model and the Empirics of Economic Growth for OECD Countries，已分类为 possible_correction；仍缺作者/摘要，不编造。2 个字段暂不可用。
- `10.1093/qje/qjag027`：官方订阅源标题为 The Power of Proximity to Coworkres，CR/OA身份核对未通过；保持原标题，不用宽松近似匹配强行写入作者/日期。2 个字段身份冲突。汇总另有 1 个 pending 身份确认问题；不能仅凭汇总把它归为本篇的标题确认问题。

当前缺 DOI 0、作者 2、月份 1、摘要 1；这 5 个问题不是全部已解决，也不等于会在每次执行时立刻重查。已尝试的字段保留冷却，身份确认仍是尚待完善的自动处理分支，不设置人工核查为正式节点。

立即复跑重新读取保存后的状态，使用全新且禁止联网的客户端：清单 verified_skipped、字段 verified_skipped，`network_calls=0`、`pointer_unchanged=true`。证明当前同批字段已完成或处在冷却期时不会发起重复请求；不宣称未知问题已清空。跨真实日期的运行尚未发生，跨日保障来自前述模拟时钟测试。

本轮 Pro 5 次、免费 SerpAPI 4 次；累计保守账本 Pro 25/2000（含历史 3 次计费不明）、SerpAPI 16/250。官方 RSS 新记录中的 15 个英文摘要进入翻译队列；旧缺摘要记录补全仍为 0，没有调用 DeepSeek。正式数据、网站和密钥配置均未更改。

下一步优先补强标题身份冲突的自动确认，并覆盖更多出版社，再接入正式每日运行。当前已验证的是扩大字段补全和持久化跳过机制，不是整个项目已全面上线。

## 2026-09-14：单词相邻字母对调的自动确认

新增极窄的自动证明规则：官方原始记录与 Crossref、OpenAlex 两份 API 记录必须 DOI、期刊一致，API 作者名单和发表月份一致，期刊 ISSN 交叉验证通过；标题只能在一个长单词里发生一次相邻字母对调。单一来源、较大标题差异、矛盾作者、月份或来源证据均不能放行。两数据库可能共享上游，所以这是元数据交叉佐证，不是独立发现覆盖证明。

补全只追加原始来源记录、填空字段，保留论文 ID、发现时间、原标题、英文摘要和已有译文。总名册新写入采用规则 v3，记录可追溯的 `identity_resolution`；旧 v1/v2 按原规则读取。自动队列增加到期标题冲突处理，不包含任意重复项合并或其他身份问题的自动放行。

真实只读验证于北京时间 2026-09-14 16:20 完成：重新读取 QJE 官网 RSS 及两 API，`10.1093/qje/qjag027` 自动确认成功，补得 Natalia Emanuel、Emma Harrington、Amanda Pallais 三位作者及 2026-05 月份；两 API 正确标题是 The Power of Proximity to Coworkers，原标题仍保留 Coworkres*。这篇属于本轮最近 60 天窗口之外，不能算窗口内漏收。

本机受限联网首次返回 ROBOTS_UNAVAILABLE 后停止；获得正常联网权限后仍按原 robots 规则成功读取，没有绕过限制。只读验证在内存执行，并通过字段写入保护重演校验；没有把结果保存至任何正式论文库。收费搜索 0、DeepSeek 0、网站部署 0。临时测试库另验证了实际持久化、重读证明和再次运行跳过，使用的是明确构造的测试数据，不冒充线上保存。

新增 5 项回归测试，包含证据缺失/篡改拒绝、旧译文保护、历史规则及持久化复用。后续仍需扩大出版社验证，并单独验证旧论文缺摘要的真实补全；尚未切换生产每日流程。

## 2026-09-14：补全流程接入已有官网摘要查询器

发现 `makeEnrichmentSources` 已有 publisher 查询器（官方订阅源、已知论文页面、受限 DOI 跳转），但新 `repairPaperMetadata` 只调用三源和搜索页面，未使用这一查询器。现在对仍缺摘要且本轮确实请求摘要的论文，先执行三源，再尝试 publisher，最后才调用既有智谱→SerpAPI 搜索兜底。查询器未提供时保持原兼容行为。

只有身份核对通过的原始来源记录才可填空。官网访问失败保留错误并继续兜底；证据存储错误立即停止。已有摘要不重复查询官网。新增 3 项离线测试验证调用顺序、成功后不搜索、受限后兜底但不采用 snippet、存储错误停止及完整记录跳过。

真实只读样例 `10.1007/s11142-026-09985-w`（RAS）于北京时间 16:27 查询：Crossref/OpenAlex 均无摘要，Semantic Scholar RATE_LIMITED，publisher REDIRECT_RESTRICTED。未补入任何摘要，没有放宽域名跳转限制。随后拟执行最多 3 篇 JPE、最多 24 个公共请求的临时副本试跑，但执行审批服务返回容量不足拒绝，命令未启动；不能报告为真实成功样例。收费搜索 0、翻译 0、生产数据和网站不变。本轮改动暂留本地，真实副本试跑和远端提交尚待继续。

## 2026-09-14：审批恢复后的 JPE 临时副本验证

用户要求继续后，审批通过，真实试跑于北京时间 16:34 完成。隔离副本中实际查询 3 篇旧缺摘要记录：`10.1086/742421`、`10.1086/742424`、`10.1086/740222`。三篇均未补齐：Crossref/OpenAlex 无新增字段，Semantic Scholar RATE_LIMITED，publisher ACCESS_RESTRICTED。不绕过官网限制，不生成或改写摘要。

本次搜索器是明确禁用联网的测试依赖，返回 `called:false`；报告中 zhipu/serpapi 的 source_unavailable 是兜底入口被跳过的状态，不代表实际 API 调用或额度消耗，也不能视为已搜索未找到。收费搜索 0、翻译 0。

副本已实际保存并重读三条摘要待办，status=source_unavailable、attempt_count=1、next_retry_at=2026-09-15T08:34:51.857Z（北京时间次日 16:34）。当前到期任务 0，三篇摘要翻译任务 0，原库全部引用文件哈希及 current 指针核对未变。其他未处理论文可能仍到期，不能据此声称整库无需重试。已验证的是官网尝试接入与失败持久化，并非摘要真实补全成功；仍需另找可访问来源验证成功路径。

## 真实搜索排查与首个旧缺摘要补全成功

手动 GitHub 任务 34850616509 实际调用 Pro 3、SerpAPI 6；三篇仍缺摘要，累计账本 Pro 28/2000、SerpAPI 22/250。诊断任务 34851660086 使用新增安全阶段日志，实际调用 Pro 3、SerpAPI 4，累计 31/2000、26/250：智谱返回的线索均被旧出版社域名规则过滤，部分返回 49 条但当时只检查前 10 条；Scholar 找到出版社页面但 ACCESS_RESTRICTED，两个旧 Google 未知结果处在冷却期，没有重复请求。

后续修复了线索检查上限（最多 50 条，仍有公共 HTTP 总预算）、智谱短标题查询和 Google 完整标题查询，并新增严格的 RePEc JPE 正式期刊记录解析。任务 34852544495 验证全线索检查生效，但搜索仍未返回可用 RePEc 记录；一次 Google TIMEOUT，其余官网访问受限。该轮 Pro 3、SerpAPI 6，最新已核实月累计 Pro 34/2000、SerpAPI 32/250。未伪造摘要，也未修改生产库。

随后将已核实的 JPE DOI→RePEc 期刊 URL 规则接为搜索前的直接查询。推导 URL 只是候选，必须逐项核对 RePEc handle 中的 DOI、URL DOI、标题、期刊、出版社、redif-article 类型、作者，以及正文摘要与 citation_abstract 一致；工作论文和任意外站不能使用。来源单独标记 repec，不冒充出版社或第四个独立发现接口。直接查询失败仍进入原真实搜索兜底。

真实临时副本 `paper-search-pilot-5duhol` 中，`10.1086/740222`（Do Mergers and Acquisitions Improve Efficiency? Evidence from Power Plants）补全成功：Crossref/OpenAlex 无摘要，Semantic Scholar NOT_FOUND，出版社 ACCESS_RESTRICTED，RePEc filled。原始英文摘要 744 字符，来源为 https://ideas.repec.org/a/ucp/jpolec/doi10.1086-740222.html 。已实际保存、重读，摘要翻译任务存在；立即复跑 skipped，原库全部文件哈希和指针不变。本次收费搜索 0、翻译调用 0。这里只验证一篇真实成功，不代表三篇或全库都补齐。

网页沿用原布局增加 RePEc 来源名称和原始链接。回归覆盖直接补全成功不搜索、直接查询失败后搜索找到第 12 条有效线索、身份/版本/摘要矛盾拒绝、旧版本兼容和来源显示。下一步接入受总预算和持久化重试保护的每日统一流程，并在正式切换前进行隔离全刊验收；当前生产开关仍关闭。

## 每日统一入口准备

新增 `runJournalPipeline`，按三源独立发现→官网清单和搜索补漏→缺字段修复→派生翻译队列执行，两个搜索阶段共享调用方提供的持久化额度服务。固定最近 60 天及每日复用规则，分阶段重读校验；沿用现有来源失败隔离，不吞掉存储、校验或账本错误。入口本身不读取密钥、不调用 DeepSeek、不操作 Git、不部署，且没有默认正式库路径。

新增 4 项测试，包括使用实际收集、保存、清单和补全函数的临时库整链测试：模拟 S2 与官网失败，CR 论文仍保存并补到模拟原始摘要，重读后队列正确。这个测试证明流程集成和保存链路，不代表已对所有期刊完成真实联网验收。CLI、每日工作流接入及全刊隔离验收仍待完成，生产开关未动。

## 2026-09-15：受双开关保护的命令与每日接入

新增 `scripts/journal-pipeline.js`：默认帮助；`--plan --all` 只读，已实际读取 545 篇原库且 production_enabled=false；`--run --isolate --all` 仅允许手动 GitHub 隔离副本；正式 `--run --save --all` 必须配置 production_enabled 和 GitHub JOURNAL_SEARCH_ENABLED 同时为真，并限默认分支。不开关不读取密钥、不构造网络服务。命令不提交 Git、不翻译、不部署。

运行时共享独立的远端月额度账本，Pro 2000/月和免费 SerpAPI 250/月不变；搜索和原始页面读取有 45 分钟截止及公共页面请求上限。源码采集保留分页上限、超时和重试。SerpAPI 每次查询先核对免费账户，不自动支付；未知计费继续占额并保留冷却。

开发分支的每日工作流及示例已接入新入口：无密钥 gate 读取双开关，新旧采集/补全步骤互斥。后续校验、提交、原 DeepSeek 翻译和网站构建顺序保留；新入口失败会传给原来源异常报告。已实际执行 gate 子进程，配置关闭时即使 GitHub 开关为真仍输出 enabled=false。新增命令/门控回归覆盖只读、拒绝非法参数、主机和分支约束、隔离失败后的原库校验、密钥隔离。以上仅准备在开发分支，尚未改变远端生产每日工作流或启用配置；全刊真实隔离验收仍待完成。

## 2026-09-15：19刊实跑结果与存档续跑修复

已只读核实 GitHub 任务 34919718346（job 104224887347）终态。固定代码 b17172b，三源发现、官网清单、字段补全均完成并提交到隔离库，各阶段返回 partial 而非全成功，因此任务结论为 failure。没有改生产库，original_unchanged=true，翻译调用0。

最后 Master List 共787条，其中研究候选767条：410条已确认在60天窗口内、254条时间仍不确定、103条已确认在窗口外。不能把787条全说成最近60天论文，也不能把相对原545条的增量全说成漏收。缺DOI 0、缺作者28、缺月份141、缺摘要205。三源独立发现并集651条，官网独有136条。上述来源覆盖是最终库聚合，不是本轮各API返回数量。

元数据阶段实际检查100篇、80篇记录发生更新；检查58篇缺摘要论文，其中31篇补到原始摘要。未解决共464篇/695项，包含pending627项、source_unavailable12项、identity_conflict56项。三源异常包括JAR OpenAlex与JOM OpenAlex各1条记录校验失败，以及JAR Semantic Scholar HTTP错误。其余来源及后续搜索没有因这些局部异常中断。

本轮真实收费/计次搜索为Pro86次、SerpAPI172次；已核实月累计Pro120/2000、SerpAPI204/250。不为获取相同日志重复发起整库任务。该次旧工作流没有上传隔离副本，因此只能从任务日志核对统计，不能声称该云端副本在任务结束后仍可恢复。

新增隔离存档命令：`--checkpoint`仅允许手动隔离模式，导出经过完整校验的历史闭包，包括来源记录、补全报告、Master List、unresolved、重试状态和翻译队列；排除锁、临时尝试、旁置密钥等非引用文件，并阻止当前运行凭据出现在导出内容。部分失败或阶段异常时只保留最后有效已提交版本，不把草稿当成果。正式库变动或快照校验失败时不导出。

`--resume-from <存档目录>`在读取搜索密钥前校验期刊配置、完整文件清单和SHA256，并复制到新临时目录中继续运行。源存档和正式库均保持不变。离线测试实际保存并恢复含原始摘要和来源证据的论文、队列及重试状态，立即复用未到期任务时网络调用0；伪造文件路径、指针字节变化、配置不符均拒绝。云端模板增加固定版本的官方上传/下载组件、30天保留期、同仓库存档续跑和失败后上传；不上传整个工作区、不下载覆盖代码、不调用翻译、不发布网站。存档模板中的代码提交占位必须替换为审核后SHA。

现有页面布局未改，只补充缺摘要的新状态说明：来源暂不可用、免费额度耗尽、身份尚未核实，并优先显示较新的持久化重试状态。后续仍须处理自动身份/日期冲突闭环、核对完整需求，以及完成真实云端存档恢复验收；新生产流程保持关闭。

## 单一来源论文自动确认闭环

发现已有 `single_source_confirmation` 待办，却被 `metadataRepairIssue` 的筛选排除。现在明确纳入自动执行；即使论文标题、作者、日期、摘要均齐全，只要该确认任务到期，仍尝试其他结构化来源，未获得可验证佐证时继续智谱→SerpAPI Scholar→Google。搜索片段不能结案，必须保存原始来源记录及其网页/API证据。

确认规则要求与最初独立发现记录比较：其他来源、同刊、规范化标题一致、DOI一致，存在作者信息时不能冲突。无DOI时，必须由原发现记录已有的作者和日期共同支持；不能把一次新查询刚填入的字段拿来反过来证明该查询自己正确。确认不会将 DOI 查询伪装为第二个独立发现来源，也不表示已覆盖整本期刊。确认后在原待办中保存resolved尝试，重复运行复用状态；身份指纹变化则重开任务。确认和缺摘要独立处理，不能因为身份确认成功就关闭尚无原文的摘要任务。

同时发现Semantic Scholar补全接口此前检查了DOI与标题，但未核实返回期刊，现复用独立发现模块的期刊身份规则；期刊缺失或不匹配时保留身份未核实并继续后续来源，不将预期刊名直接当作远端证据。旧tldr不作摘要的测试仍保留，增加错误/缺失期刊拒绝断言。

新增8项针对性测试覆盖结构化成功、三源失败后的完整搜索顺序、原始页面确认、同源不能自证、无DOI自证防护、持久化和零重复请求、身份变化重开、身份已确认但摘要缺失，以及免费额度重置重试。运行统计增加single_source_confirmed。标题冲突原有回归继续有效；可能重复、日期冲突与文献类型不确定的自动处理仍待完善，不能称为全需求完成。

隔离存档代码4a2e7d4已推送开发分支；默认分支的手动验收工作流更新被权限审批拒绝，已向用户请求仅限该文件的授权，未绕过拒绝。此处修改继续只在开发分支完成，不依赖该授权，也未调用收费接口或发布网站。

## OpenAlex拒收排查与原始摘要校验统一

2026-09-15对JAR、JOM各进行1次公开OpenAlex查询（无密钥、无收费搜索），分别返回10、5条。复现上轮各1条拒收：JAR `W7170193365` / `10.1111/joar.v64.4`，JOM `W7211935787` / `10.1002/joom.v72.6`，两者title均为null。拒收原因是必需标题缺失，不是摘要缺失。第一次进一步类型查询被审批服务容量不足中断；随后只读查询两条DOI的Crossref记录成功，均明确type=journal-issue、title=[]，分别为64卷4期、72卷6期，ISSN也与配置吻合。因此这两条具体异常是整期期刊记录，不是已证实漏收的研究论文。未编造标题入库，也未把DOI样式泛化成排除规则。

代码检查发现倒排摘要校验不一致：补全入口拒绝位置断裂/重叠，日常独立收集入口却会拼接剩余单词。现统一使用严格解析器，要求位置从0连续、每个位置恰有一个词、结构及数量有效。错位、重复、空白词、非法数组/位置全部拒绝作为摘要。

日常收集遇到该错误时保留身份和标题有效的论文，abstract留空，写入 `INVALID_ABSTRACT_INDEX` 警告，交由既有缺摘要自动修复流程继续处理。原始响应完整保存在来源快照，警告包含原始页展开后的记录索引和字段名；运行来源明细增加可选 `metadata_warning_count`，旧日志保持兼容。论文采集成功不等于元数据完整，缺摘要数量仍反映在Master List和unresolved。补全入口采用同一严格解析器，继续拒绝损坏摘要并进入其他来源。缺标题、期刊不匹配等身份错误的原拒收规则未放宽。

已完成的回归验证包括：重建正常词序，拒绝断裂/重复/错误结构；损坏摘要不会丢掉整篇论文；有效Crossref摘要不被覆盖；实际保存后原始页及警告可重读、待补全任务存在、没有中文摘要或摘要翻译任务。本修复不追溯改写已经保存的历史原文，尚不能声称历史库中每条摘要均已重新审计。

## 月份显示与日期冲突自动重试

确认总名册按月份推导，但网页仍使用旧规范日期字段。新增公开publication_month/publication_year、日期类型和来源署名；日期显示沿用原卡片布局，统一到月份，只有年份则显示月份待补，冲突则明确待自动核实。在线/纸刊字段也从每个来源的最新有效证据派生，不能继续显示来源更正前的旧值。旧网页数据仍可读取并按月展示。不修改原始历史日期，不截短首次发现日，不改变日历筛选、原文、已有中文或翻译逻辑。

publication_month_conflict现纳入自动修复执行；与同篇missing_publication_month合并成一次字段请求。来源后来更正且已不存在冲突时，由保存证据重算总名册并自动关闭两项待办；仍有矛盾时继续三源与既有搜索兜底，保存identity_conflict及退避重试时间，不能按多数票或猜测选择月份。额度耗尽仍优先保留quota_exhausted。

本阶段的真实临时库测试覆盖年月精度、原始日信息保留、冲突时不显示任意旧月份、来源更正后保存与公开数据同步、未解决时完整搜索顺序与冷却；前端DOM测试验证已知月份、年份待补和月份冲突三种显示，发现日保持不变。这不等于所有日期冲突都能解决，无法取得可靠证据的记录仍自动重试。

## 未解决清单的集中处理接口

`node scripts/master-list.js --unresolved` 仍为只读、不联网、不读取密钥的命令。输出升级到schema_version=2，保留字段问题、官网清单待办和未确认候选；字段问题补充作者、期刊、年/月、来源标记、可安全访问的证据链接及最近一次修复的分来源尝试。只导出必要字段，不携带网页/API原始正文、额外诊断对象或带密钥参数的链接。最近尝试附有时间，不能将旧失败解释成当前所有来源仍然失败。

字段问题支持 `--limit 20 --offset 0` 分页，issue_count/paper_count表示筛选后的总量，returned_issue_count表示当前页；has_more明确是否还有下一页。分页按问题ID排序，不代替自动执行器的优先级。官网清单待办与未确认候选仍全部输出，通过catalog_pagination=not_paginated明确区分。修复原到期查询先截取10000项再过滤期刊的问题，超过10000项也能读取后续页。

多页查询需核对snapshot_sha256一致；若包含 `--due`，下一页传入首份输出的 `--as-of <query_at>`，避免翻页期间新任务到期导致结果挪位。若版本改变则重新读取，不将两个版本的页拼接。该接口供后续Codex集中处理使用，不增加人工核查节点，也不自动启动收费搜索。

新增4项测试覆盖必要上下文与敏感数据过滤、10005项跨页读取、期刊过滤、固定查询时刻和非法参数拒绝。实际开发库只读调用返回379项到期问题，按limit=2输出2项且has_more=true，数据版本和证据链接均存在。此数量仅是当前开发库的问题数，不是漏收论文数，也不是线上最新统计。

## 自动执行器的大队列选择修复

继只读导出修复后，发现自动字段执行器和试跑复用检查仍会先截取10000项再筛选期刊、论文ID和可处理类型。这会使后面的目标误报NOT_DUE，也可能只选中同篇论文的部分字段。现共用dueMetadataRepairIssues，先检查到期状态、启用期刊、目标ID和支持的问题类型，保留所有符合条件的问题，再按既有优先级选择最多maxPapers篇论文；同篇所有到期字段一起处理。未到期任务仍不提前请求，原论文批量上限、额度上限及重试规则不变。

新增5项回归覆盖超过10000项的期刊及ID筛选、同篇身份与摘要跨截断边界、未到期/已解决/停用期刊排除、排序与默认限量兼容、非法参数拒绝。全套420项测试通过。这是模拟大队列验证，不代表线上已有这么多待办；没有调用收费API、重跑整库或开启生产流程。此阶段possible_duplicate与document_type_uncertain仍未接入自动执行，本修复不将它们误报为已解决。

## 2026-09-16：文献类型自动查询与可追溯更正

document_type_uncertain现纳入字段执行器；三源查询仍无法明确时，继续智谱、SerpAPI Scholar、Google，保存not_found等真实失败状态及退避重试，不要求人工核查。即使标题、作者、月份、摘要全部已有，类型问题仍会触发查询。Semantic Scholar补全请求增加publicationTypes，并与独立发现共用类型映射；Editorial、Review及JournalArticle按原字段保存，未知或缺失不再硬填journal-article。

分类可使用同一来源、同一source_id、同刊、同DOI及规范化标题的较新明确类型。必须有相符的结构化API方法、官方域名、响应哈希和抓取时间；同刻记录、缺失类型、旧更新时间倒退、身份不同或其他来源仍冲突，均不消除警告。原始历史记录不覆盖，撤稿/更正通知不会因后来的普通类型而消失。出版社页面的默认文章类型不能充当覆盖旧分类的证明；本次不承诺解决所有类型歧义。

新总名册使用规则v4，v1/v2/v3仍按各自历史规则重建和校验。针对性测试实际创建临时库，验证来源更正→保存→重读→关闭待办→公开分类同步→重跑零请求；还覆盖三源失败后完整搜索顺序和持久化冷却、类型/身份/日期/证据防护、Semantic Scholar原始类型读取。既有UI布局、搜索日历和DeepSeek翻译实现未改，旧中文及原文保留。开发库545篇旧数据只读校验通过；这不是线上数据更新，也不是实际联网验收。此阶段疑似重复条目的自动处理和真实云端存档续跑仍未完成。

## 疑似重复自动核实及逐问题冷却保护

只读检查开发库发现2条possible_duplicate，都是JAE的Editorial data编务资料，已按原分类排除于研究待办之外，不能据此声称存在2篇重复研究论文。代码层面此前possible_duplicate未纳入执行，现将其加入到期筛选，即使基础字段已齐全也执行结构化查询和智谱→SerpAPI Scholar→Google，仍不明确时保存identity_conflict或quota_exhausted及真实重试时间。不会因为identity字段没有实现重复检查而返回resolved。

查询范围保持同刊、规范标题一致、年份相同或暂未知。补到原始日期证据、已能区分年份时，Master List与双方重复待办自动同步，保留两篇论文及原文、已有译文、首次发现时间。无法区分时报告关联paper_id与DOI；只读unresolved导出附带关联记录当前的标题、作者、期刊和月份，不泄露网页正文。关联记录不在库中时不导出悬空引用。

同时修复失败记账按field粗略匹配的问题：单源确认、标题冲突和疑似重复均属identity字段，执行其中一项不能重置另一个尚未到期问题的冷却。现在只对本轮真正到期且选中的issue_id记录尝试；可靠证据自然解决其他问题时，仍允许正常自动关闭。

新增6项测试覆盖范围与Master List一致、日期误报排除、完整搜索链、额度耗尽、同字段不同待办冷却隔离，以及已有DOI冲突不覆盖删除。这是自动查询、误报排除和重试闭环，不是已入库重复记录的自动合并完成；DOI_ALREADY_ASSIGNED仍保留为未解决。跨历史ID的安全合并须保留来源、译文和发现信息，不能用删除记录或放松历史校验代替。

## 安全合并证明、核心算法及可恢复证据

新增duplicateMerge核心：仅对无DOI原记录与已有DOI主记录提出归并。要求同刊、长规范标题一致、真实API/官方页面证据、作者和年月无矛盾、没有未解决的标题冲突、不是通知/编务类型。证明必须连到原发现记录的稳定来源编号，或由原发现记录已有的作者与日期共同支持，不能用本次刚补的作者字段自证。未来证据、时间倒退、未知主记录身份及不同DOI均拒绝。

算法返回完整保留的原记录、吸收来源与缺失字段的主记录和可重演resolution，尚不改现有持久化结构。主记录已有英文与中文不覆盖；原记录全部来源保留；已完成中文仅在对应英文哈希完全一致时复用，其他版本仍留在原记录中。新获得的真实摘要仍按原pending翻译规则处理，不生成中文。分别保存两条历史发现时间，并在resolution中派生最早发现日；只有日期的旧记录不会虚构时分秒。

元数据查询遇DOI_ALREADY_ASSIGNED时保留候选证据，允许将该DOI作为后续结构化查询的临时线索，不写入无DOI论文。后续API返回的稳定编号若能与原记录相连，则形成merge_ready报告并停止不必要的收费搜索；原始标准化证据随修复报告保存，重读后可恢复。merge_ready不等于已经归并：当前仍保留两篇和未解决待办，不提前宣称成功。持久化归并事务、后续采集对旧身份的重定向、Master List/展示/翻译队列的统一去重仍需接入。

新增验证涵盖稳定编号、原作者日期防自证、来源/身份/时间拒绝、原文和译文保留、原始摘要补入、日期精度保真，以及模拟三源查证→保存报告→重读证据。仅离线测试，没有调用收费接口或改动线上数据。

## 可重演的归并保存与每日流程接入（开发版）

新增独立duplicate_resolution保存操作，消费已保存的原始身份证据，不联网、不计搜索费。当前名册移出被归并身份，但完整原记录、主记录归并前版本、证明、旧待办及旧摘要重试状态写入不可变归并报告；父版本和其引用文件全部保留。主记录保留已有DOI和ID，首次发现信息调整为两条记录中确实更早的发现；各自原始时间仍可在归档中追溯。日精度历史不虚构精确时间。

普通采集、补全和翻译仍禁止删除历史记录。只有明确归并操作可通过特殊校验：写入前及重新读取时均从真实父版本完整重演，要求整个新名册及归档字节内容与算法结果一致，且数量减少恰等于归并数。不得借机删除其他论文、覆盖译文或改变其他待办冷却。原记录移出的待办只从当前工作队列退休，历史状态完整归档。归并证据若指向多个不同DOI目标，继续保留未解决，不按查询顺序或新旧先后强选。

统一每日入口在字段补全前复用上次已核实的归并证据，在字段补全后处理本轮新证据，两次合计不超过maxPapers。元数据阶段已有可靠归并计划时，不再对目标主记录重复请求；最终问题确实消失后才标记resolved_after_merge，不将merge_ready直接当成功。保留原始阶段状态，并增加归并数统计。官网独立发现标记沿归并关系保留，不将普通查询变成官网发现。没有修改UI文件、日历筛选实现或DeepSeek翻译实现。

新增9项实际临时库测试：完整保存与重读、原记录和旧译文留档、最早发现时间、再次采集无DOI旧来源不复活身份、过期旧ID译文不写错论文、中途失败保留旧指针、篡改拒绝、重启复用零补全请求、新证据不重复查目标、不同DOI目标冲突保留，以及隔离存档导出/恢复保留归档和父版本。测试中归并前2条当前记录变为1条，历史档案仍可读取；这是模拟库验证，并非在线数据库已发生归并。

只读运行计划仍显示production_enabled=false，开发库545条，60天窗口及Pro 2000/SerpAPI 250月上限未改。新入口代码已接入归并流程，但远端生产开关与每日任务未切换。真实云端存档恢复验收及基于最新线上数据的上线前核对仍待完成；更新默认分支手动验收任务仍需用户明确授权。

## 2026-09-16：最新生产快照兼容性核对（非上线）

固定生产提交 `74cf941ae26219c3805fc5dd04083adf63b1b77c`，只读下载其当前快照引用的81个文件（约9.2MB）到独立临时目录，并逐个核验SHA-256。未下载完整历史父版本链，本次不声称验证完整历史存档闭包。新代码完整读取当前快照、相关补全报告及翻译队列通过：555条记录、15次采集日志、63次翻译导入日志、9次补全日志。线上与开发版19刊配置语义一致，仅文本格式不同。

另建隔离副本，禁止网络请求，模拟一篇待办的三源不可用与搜索无结果。执行顺序实际为Crossref→OpenAlex→Semantic Scholar→智谱→SerpAPI Scholar→Google；这只是离线依赖模拟，不代表真实服务请求成功或计费。补全事务保存后重新读取通过，总名册升级为v4，全部555条论文原始内容和已有译文逐字段一致，原下载快照未改。无有效归并证据时归并阶段正确跳过，不能强行合并。完整自动测试再次449项通过，0失败。

按v4对这个固定生产快照推导：538条研究候选，138条记录缺摘要，11条缺作者，0条缺DOI，380项自动待办。待办包含不同类型的问题，不等于380篇漏收论文；这也不是三源加搜索新流程跑完后的成果。该快照尚无Semantic Scholar独立发现记录。最近一次生产采集的partial_failure来自OpenAlex的JAR、JOM各1条未通过校验的记录，不能据此断言存在2篇漏收研究论文。

远端手动验收任务仍固定旧代码 `b17172b820cb328f66acb9cca6b640779210565a`，没有试跑快照上传或续跑步骤；每日生产任务仍为原有双源采集加官方补全。下一步需要明确授权更新默认分支的手动验收任务，固定经过审查的开发提交，启用隔离结果存档，再做真实联网及云端恢复验收。该授权不包含修改每日生产任务、开启新版生产开关或发布网站。本轮未读取密钥、未调用收费搜索、未修改远端配置或线上数据。

### 验收代码与最新数据分离

随后检查发现，固定待验收代码提交会同时固定其附带的旧数据库。开发版验收模板现将默认分支当前论文库单独检出到production-baseline，代码仍固定审核提交，两者不相互覆盖。统一入口新增仅隔离运行可用的`--source-repository`：先校验选定数据基线、再复制至临时目录，所有采集补全写入副本；正式保存及只读计划不接受该参数，基线损坏不会回退到旧库或启动收费服务。报告附带基线论文数量与快照哈希。续跑仍使用已验证的存档，不把两套数据静默混合。

新增入口测试验证555条模拟基线的路径选择、复制与写入隔离、报告指纹、无效参数及读取失败保护，模板测试验证默认分支数据、独立目录和禁用凭据持久化。完整回归451项通过，0失败。该模板尚未应用到远端手动任务。

针对固定生产提交的完整父版本链另作只读补取，初次连接中断，复用已校验文件并按每个请求最多三次重试后，仍因连接超时退出。临时副本保留85个文件，尚未完成完整历史链的存档恢复验证；不能将当前快照兼容通过等同于最新生产全历史恢复通过。没有修改线上数据或消耗搜索额度。此项需网络恢复或授权后的云端验收继续完成。

### 用户授权后的上线候选准备

用户明确授权“补充完这个检验就上线”。手动验收任务已更新为固定开发提交3a222824a0392bc4b10d4f05ecb499d5dc67c66b，任务35116894166于2026-09-16T15:40:14Z启动，独立检出最新生产数据、451项离线测试通过后进入实际采集。此处仅记录启动，不代表验收完成或新版已上线。

上线候选的配置开关准备为true，但实际执行仍要求GitHub仓库变量JOURNAL_SEARCH_ENABLED精确等于true。原测试把生产配置永久为false当作断言，会使正常上线后的测试误报失败；现测试真实配置，并以独立配置文件验证false/true与仓库变量空值、false、true、TRUE的全部组合，只有两个开关同时开启时通过。不会降低额度或密钥保护。最新生产快照555条在隔离临时目录构建静态页面通过，不发布该本地副本。

正式发布必须以当时最新master树为基础，只加入审查过的程序、测试、文档和工作流文件；不得把开发分支附带的旧data目录合并覆盖生产。实际采集产出、搜索记账分支、已有译文与所有历史快照保持原位。待当前云端输出及下载存档恢复核验后，才允许切换生产并检查实际网站。
