# 网络中断后的有限翻译重试

用户已明确授权必要的重复 API 调用。MS `10.1287/mnsc.2022.03251` 的标题和摘要在 2026-09-10 出现已结束的 `NETWORK_ERROR`，原有逻辑永久暂缓，导致真实英文已齐全但中文一直缺失。

现在仅对已结束、状态为 failed、用量未知且错误为 NETWORK_ERROR / TIMEOUT 的请求，在至少 24 小时后允许重试。旧调用可能已产生费用，仍按未知用量保留，不清空、不退款、不冒充零费用。每个原文任务累计三次网络失败后停止，不因换请求方案而重置；原有每方案三次和总共九次的上限仍适用。

新调用继续先把关联上一笔失败的新预登记上传，再请求 DeepSeek。成功字段不重发，未结束的预登记永不自动重发；账户、权限、模型、用量缺失和其他错误不适用此例外。英文摘要缺失时仍不能生成中文摘要。

此文更新 2026-09-17 恢复说明中“所有网络失败永久暂缓”的规则，不改变其历史记录。智谱欠费停止保护和 SerpAPI 每月最多 250 次免费额度均不变。
